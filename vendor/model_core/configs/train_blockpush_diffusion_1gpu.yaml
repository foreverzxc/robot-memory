# Block Push diffusion policy, single-GPU embedding-precompute run.
defaults:
  - train_blockpush_diffusion
  - override /encoder: dino_patch
  - override /cluster: local
  - _self_

batch_size: 128
epochs: 300 
eval_freq: 5
eval_on_env_freq: 20
precompute_embeddings: true
num_workers: 0
